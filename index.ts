/**
 * Exercise
 * 1. Define a function that has two parameters, a number and a string
 *    Add a return type of number as well
 * 2. Call your function with the correct parameter types
 * 3. Call your function with the incorrect parameter types
 *    Notice the error
 * 4. Change your function to return a type that is different than the declared type
 *    Notice the error
 */

